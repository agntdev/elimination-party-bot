[
  {
    "name": "Join auto-creation path still reports missing persistent storage",
    "steps": [
      {
        "send": { "callback": "join:round" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "editMessageText",
            "payload": {
              "text": "Round storage is not configured. Set REDIS_URL before joining rounds."
            }
          }
        ]
      }
    ]
  }
]
