[
  {
    "name": "/join reports missing persistent storage",
    "steps": [
      {
        "send": { "text": "/join" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Round storage is not configured. Set REDIS_URL before joining rounds."
            }
          }
        ]
      }
    ]
  }
]
