[
  {
    "name": "/leave reports missing persistent storage",
    "steps": [
      {
        "send": { "text": "/leave" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Round storage is not configured. Set REDIS_URL before leaving rounds."
            }
          }
        ]
      }
    ]
  }
]