[
  {
    "name": "Leave button reports missing persistent storage",
    "steps": [
      {
        "send": { "callback": "leave:round" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "editMessageText",
            "payload": {
              "text": "Round storage is not configured. Set REDIS_URL before leaving rounds."
            }
          }
        ]
      }
    ]
  }
]
