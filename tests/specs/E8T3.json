[
  {
    "name": "/round reports missing persistent storage",
    "steps": [
      {
        "send": { "text": "/round" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Round storage is not configured. Set REDIS_URL before viewing rounds."
            }
          }
        ]
      }
    ]
  }
]